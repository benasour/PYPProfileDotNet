@model PYPProfileDotNet.Models.Friend

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Friend</legend>

        @Html.HiddenFor(model => model.id)
        @Html.HiddenFor(model => model.user_id1)
        @Html.HiddenFor(model => model.user_id2)

        <div class="editor-label"> Status:
            @Html.LabelFor(model => model.status_id)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.status_id)
            @Html.ValidationMessageFor(model => model.status_id)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
